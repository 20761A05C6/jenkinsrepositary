pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "its a build job"
            }
        }
        stage('Test') {
            steps {
                echo "its test job"
            }
        }
        stage('Deploy') {
            steps {
                echo "its deploy job"
            }
        }
    }
}
